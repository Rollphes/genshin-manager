/**
 * Metadata for generated schema and enum files
 */
export interface GenerationMetadata {
  /** Source commit SHA */
  commitId: string
  /** Generation timestamp (ISO 8601) */
  generatedAt: string
}
