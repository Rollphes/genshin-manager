name: ReviewDog

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  code-quality-check:
    name: Code Quality Check (ESLint, TypeScript)
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false
    permissions:
      contents: read
      pull-requests: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        # Remove cache temporarily to avoid lock file issues
        # cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Setup reviewdog
      uses: reviewdog/action-setup@v1
      with:
        reviewdog_version: latest

    - name: Run ESLint with reviewdog
      uses: reviewdog/action-eslint@v1
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        filter_mode: diff_context
        fail_level: error
        eslint_flags: '.'

    - name: Run TypeScript type check
      run: npx tsc --noEmit

    # docs-site checks (separate ESLint config)
    - name: Install docs-site dependencies
      run: npm ci
      working-directory: docs-site

    - name: Run ESLint for docs-site
      uses: reviewdog/action-eslint@v1
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        filter_mode: diff_context
        fail_level: error
        eslint_flags: '.'
        workdir: docs-site

    - name: Run TypeScript type check for docs-site
      run: npx tsc --noEmit
      working-directory: docs-site